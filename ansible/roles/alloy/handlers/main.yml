---
# Alloy role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart Alloy
  ansible.builtin.debug:
    msg: "Alloy configuration updated - container will use new config on next restart"
  changed_when: false

